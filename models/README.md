# Models Folder

This folder is reserved for trained model weights.

Expected future files:

- `alignn_solid.pth`
- `alignn_liquid.pth`

These files are not available yet because the current datasets do not include the atomistic structure files required for official ALIGNN training.
